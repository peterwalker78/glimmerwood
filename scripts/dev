#!/usr/bin/env bash
# Build and run Glimmerwood from the source tree. Arguments are passed to glimmerwood.
source "$(dirname "$0")/lib.sh"
require_box "$@"
cd "$ROOT"

scripts/build-ui
exec cargo run -- "$@"
